a = 1
k = 3
# 末位增加一位且为1
b = (a << 1) + 1

# 将最后一位变为1
b = a | 1

# 将最后一位变为0
b = (a | 1) - 1

# 将最后一位取反
b = a ^ 1

# 将右边第k位变为1
b = a | (1 << (k - 1))

# 将右边第k位变为0
b = a &(~(1 << (k - 1)))

# 将右边第k位取反
b = a ^(1 << (k - 1))

# 取右边k位
b = a & ((1 << k) - 1)

# 取右边第k位
b = (a >> (k- 1)) & 1


print(b)